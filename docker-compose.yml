version: "3.8"

services:
  frontend:
    build: ./frontend
    container_name: token-faucet-frontend
    ports:
      - "3000:3000"
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
